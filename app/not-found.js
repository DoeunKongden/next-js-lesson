export default function NotFound(){
    return(
        <main className="not-found">
            <h1>Not Found</h1>
            <p>Unfortunately, we can't find the requested page or resources.</p>
        </main>
    )
}